import useForm from './../src/index';

describe("test", () => {
  it("should do stuff", () => {
    const formSchema = {
      name: { initial: '', isRequired: true, placeholder: 'Alex' }
    }

  });
});
