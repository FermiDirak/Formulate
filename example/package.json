{
  "name": "example",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.3.2",
    "@testing-library/user-event": "^7.1.2",
    "flow-bin": "^0.118.0",
    "formulate": "link:../packages/formulate",
    "react": "link:../packages/formulate/node_modules/react",
    "react-dom": "^16.12.0",
    "react-highlight.js": "^1.0.7",
    "react-scripts": "3.3.1"
  },
  "scripts": {
    "sync-formulate": "cp -r ./../packages/formulate/src/ ./src/formulate",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "yarn build; netlify deploy --prod -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
